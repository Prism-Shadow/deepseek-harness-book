# 第 8 章配套资料

本目录保存第 8 章求职案例使用的输入材料和提示词。按照正文操作时，新建 `job-hunt` 工作区，在标准模式下打开会话，将 `source/parttime-notes.md` 放入工作区，再按 `prompts.md` 的顺序发送提示词。

提示词保留了实测中的修正过程。生成文件后仍要核对数字、职责和成果归属。模拟面试部分请根据自己的经历回答，不要照搬演示材料。

本目录只保留复现实操所需的输入，不包含虚拟环境、缓存、截图和生成结果。正文图片位于 `book/assets/chapter8/`。
