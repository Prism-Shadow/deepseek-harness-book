# 第 9 章实操与截图指导（与 book/chapter9.md 逐步对应）

照此文档从上往下操作即可，提示词都是正文里的原文，直接复制粘贴。全部提示词已在写作机上用 headless 模式全链路跑通（留痕在 `~/dev/liuzh/dsh/dsh-work/第九章/实测记录/`），web 端预期顺畅；下文标注"CLI 实测"的提示是从那次实测里总结的。截图存到 `book/assets/chapter9/`，文件名照写。除了截图，请顺手记录每一步 dsh 的实际表现（它报的数字、有没有追问、有没有报错重试），发给我用于回填正文里的「待实测」。

通用注意：截图前确认画面里没有 API Key、个人路径、无关聊天；图不要经聊天软件二次压缩。

## 准备

1. 启动 dsh：终端运行 `npx -y @deepseek-ai/dsh web`，浏览器打开它打印的地址。
2. 单击左侧 **Workspaces** 旁的 **Add workspace**，新建文件夹 `a-share-research` 作为工作区。
3. 选中该工作区，新开一个会话，确认是 **Standard mode**。

## 第一步　取行情数据（对应 9.1 第一段提示词）

发送：

```text
这个工作区用来搭一个跟踪上证指数的投研助手，第一步先准备行情数据。

安装 akshare（需要的话自己创建 Python 虚拟环境），用它的
stock_zh_index_daily 接口取上证指数（代码 sh000001）的全部历史日线，
保存为 data/index_daily.csv。

完成后汇报数据的起止日期、总行数、每个字段的含义，以及最新一个交易日
的日期和收盘价。先不要做任何分析。
```

看什么：dsh 应当自己建环境、装 akshare、写脚本、运行、汇报。如果它中途报错又自己修好，这段过程本身有价值，一并截下来。

拍图：

- `9-1-01-fetch-running.png`：执行过程的消息流，能看到建环境、写脚本、跑脚本的工具调用。
- `9-1-02-data-summary.png`：dsh 汇报数据概况的回复（起止日期、总行数、字段、最新收盘价）。

记录给我：它报的起止日期、行数、最新交易日和收盘价（我要写进正文，参考值约为 1990-12-19 起、八千多行）。

核对：打开 `data/index_daily.csv` 看行数字段对不对得上；拿最新收盘价跟任一行情页对一下。

## 第二步　数据体检（对应 9.1 第二段提示词）

发送：

```text
对 data/index_daily.csv 做一次数据检查，逐项确认，日期有没有重复或乱序、
数值字段有没有缺失或明显异常、相邻交易日之间有没有可疑的大缺口，注意
区分节假日的正常空档和真正的数据缺口。

把检查过程和每一项的结论写成 data/数据检查.md。不要修改数据，只汇报。
```

拍图：

- `9-1-03-data-check.png`：dsh 汇报检查结论的回复。

记录给我：它查出了什么（有无异常、怎么区分节假日空档的）。

## 第三步　让 dsh 复述规则（对应 9.2 第一段提示词，同一会话继续）

发送：

```text
接下来把一条经典的双均线方法变成可以检验的规则。原始想法是，短期均线
上穿长期均线说明市场转强，下穿说明转弱，短期用 20 日，长期用 60 日。

先不要写代码。把这个想法复述成一条精确的规则，说清楚均线用哪个价格、
按什么方式计算，"上穿"和"下穿"的确切判定条件是什么，信号记在哪一天。
我没说清楚、需要我拍板的地方，列出来问我。
```

它大概率会列出几个待确认点问你（CLI 实测它问了六个，逐条对应如下）。回复时按这套口径拍板，与正文口径一致：

- 价格用收盘价（它会提典型价备选，不采用）；
- 简单移动平均，窗口含当日；
- 上穿判定为前一交易日短均线小于等于长均线、当日短均线大于长均线，下穿相反；两线恰好相等视为未穿越；
- 信号记在穿越发生的当天（收盘后确认）；
- 成交时点它可能建议用次日开盘价，回答用次日收盘价（正文 9.3 回测就是这个口径，别顺着它的建议走）；
- 只做多不做空，先不计交易成本和滑点（这两条 9.3 的回测提示词里也会再写一遍）。

拍图：

- `9-2-01-rule-restate.png`：dsh 的复述回复，最好能看到它列出的待确认点。

记录给我：它问了什么、你怎么答的。

## 第四步　实现规则并出信号（对应 9.2 第二段提示词）

发送：

```text
按刚才确认的规则实现。先把最终确认的规则定义完整写成 signals/规则定义.md，
后面的每一步都以这份文件为准。

然后计算 20 日和 60 日均线，在全部历史数据上标出所有向上信号和向下信号，
生成一份信号清单，包含日期、信号方向、当天收盘价，保存为
signals/信号清单.csv，并汇报信号总数和最近五条信号。

再画一张最近三年的走势图，包含收盘价、两条均线和全部信号点的标记，
保存为 signals/近三年信号图.png。

这些信号只是规则的触发记录，下一步要拿历史数据检验，所有输出里都不要
出现买卖建议的措辞。
```

拍图与取图：

- `9-2-02-signals-list.png`：汇报信号总数和最近五条信号的回复。
- `9-2-03-signal-chart.png`：把工作区里 `signals/近三年信号图.png` 直接复制过来改成这个名字（产物图，不用截屏）。

核对：打开 `signals/信号清单.csv` 扫一眼用词，只该有「向上信号」「向下信号」。

记录给我：信号总数、最近五条信号的日期和方向。

## 第五步　回测（对应 9.3 提示词，同一会话继续）

发送：

```text
对 signals/信号清单.csv 做一次历史回测，信号定义以 signals/规则定义.md
为准，行情数据用 data/index_daily.csv，回测规则如下。

只做一个方向，出现向上信号后，从信号次日的收盘价开始计入持有，直到
向下信号出现后的次日收盘价结束，其余时间空仓。不考虑交易成本和滑点，
每段持有都用全部资金模拟。

统计以下内容，持有段数、每段的涨跌幅、上涨段占比、全程累计净值、最大
回撤，并和同期从头持有到尾的基准做对比。把统计结果和一张净值对比图
整理成 backtest/回测报告.md，图片保存在 backtest/ 目录下。

报告结尾单独加一节，列出这条规则表现最差的两三个时间段，说明当时的
行情是什么样子。
```

拍图与取图：

- `9-3-01-backtest-report.png`：dsh 汇报回测结果的回复（持有段数、上涨段占比、累计净值、最大回撤等数字要在画面里）。
- `9-3-02-equity-curve.png`：把 `backtest/` 下的净值对比图产物直接复制过来改名（不用截屏）。

记录给我（最重要的一批数字，正文解读全靠它）：持有段数、上涨段占比、策略累计净值、基准累计净值、最大回撤，以及报告列出的最差时段和它的说明。把 `backtest/回测报告.md` 全文发我最好。

先打个预防针，CLI 实测的结果是策略累计净值明显跑输从头持有的基准、上涨段占比不到四成，你大概率也会看到类似的"难看"结果。这不是哪步做错了，恰恰是 9.3 要的检验结论，照实截图记录就好。

## 第六步　挂载 schedule 插件（对应 9.4 第一小节）

1. 关掉 dsh（终端 Ctrl+C）。
2. 打开 `~/.dsh/profiles/web/cordis.patch.yml`（`$DSH_HOME` 默认就是 `~/.dsh`），在文件末尾追加：

```yaml
- insert:
    - id: schedule
      name: '@deepseek-ai/dsh-schedule'
```

3. 重新运行 `npx -y @deepseek-ai/dsh web`，回到 `a-share-research` 工作区，**新开一个会话**。
4. 顺手帮我验证一条待确认项：先恢复打开之前的旧会话，发一句「列出当前所有定时提醒」，看它有没有 schedule 工具可用，记录结果；然后回到新会话继续。

在新会话发送：

```text
列出当前所有定时提醒。
```

拍图：

- `9-4-01-schedule-list.png`：dsh 调用 `schedule_list` 返回空清单的回复。

## 第七步　固化晨报脚本（对应 9.4 第二小节，新会话继续）

发送：

```text
把每天早上的例行动作固化成一个脚本 daily_report.py，执行三件事，用
akshare 把 data/index_daily.csv 增量更新到最新交易日，按
signals/规则定义.md 里确认过的双均线规则重算最新的信号状态，在
reports/ 下生成一份以日期命名的晨报。

晨报只陈述数据事实，包含最新收盘价和当日涨跌幅、20 日和 60 日均线的
当前数值、当前处于向上还是向下信号区间、距离上一次信号过去了多少个
交易日。不写任何操作建议。

写完手动执行一次，把生成的晨报全文给我看。
```

拍图：

- `9-4-02-first-report.png`：第一份晨报全文。

核对：晨报里只该有数据事实，出现建议式措辞就让它改脚本再重拍。

记录给我：晨报全文（文字版）。

## 第八步　演示提醒并等触发（对应 9.4 第三小节）

发送：

```text
建一个演示用的定时提醒，每 5 分钟一次，提醒内容是执行 daily_report.py
并汇报晨报要点。建好后告诉我这条提醒的编号。
```

然后等 5 分钟，页面别关。

拍图：

- `9-4-03-reminder-fired.png`：提醒触发后 dsh 自动执行脚本并汇报的完整过程（触发消息和它的执行都要在画面里）。

记录给我：触发在界面上长什么样（是一条什么样的消息）、触发时刻和创建时刻差多少（帮我确认锚点语义）。

## 第九步　换成正式每日提醒

发送：

```text
把刚才的演示提醒删掉，重新建一个每 24 小时触发一次的提醒，内容不变，
执行 daily_report.py 并汇报晨报要点。
```

拍图：

- `9-4-04-daily-created.png`：删除演示提醒、创建每日提醒成功的回复。

## 第十步（可选，帮我验证 cron 兜底）

不用真配 crontab，在终端手动跑一次这条命令就行（把路径换成你的工作区）：

```sh
cd <工作区路径> && npx -y @deepseek-ai/dsh --profile headless "执行 daily_report.py，把生成的晨报全文打印出来"
```

记录给我：它有没有成功执行脚本并打印晨报（这决定正文 cron 一段怎么定稿）。不用截图。

## 完成后

10 张截图加 2 张产物图放进 `book/assets/chapter9/`，连同各步记录发我。我负责回填正文的「待实测」、恢复图片引用、重跑构建和终稿检查。
